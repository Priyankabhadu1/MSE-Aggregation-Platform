<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>MSE Connect | Sign In</title>
  <link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@600;700&family=DM+Sans:wght@300;400;500;600&display=swap" rel="stylesheet">
  <style>
    :root {
      --saffron: #FF6B35;
      --deep-navy: #0D1B2A;
      --teal: #00897B;
      --teal-light: #00BFA5;
      --gold: #FFB300;
      --cream: #FFF8F0;
      --text-main: #1a1a2e;
      --text-muted: #6b7280;
      --card-bg: #ffffff;
      --border: #e5e7eb;
    }

    *, *::before, *::after { margin: 0; padding: 0; box-sizing: border-box; }

    body {
      font-family: 'DM Sans', sans-serif;
      min-height: 100vh;
      background: var(--deep-navy);
      display: grid;
      grid-template-columns: 1fr 1fr;
      overflow: hidden;
    }

    /* ── LEFT PANEL ── */
    .left-panel {
      position: relative;
      display: flex;
      flex-direction: column;
      justify-content: center;
      padding: 60px 56px;
      overflow: hidden;
      background: linear-gradient(145deg, #0D1B2A 0%, #112240 60%, #0a3d2e 100%);
    }

    .left-panel::before {
      content: '';
      position: absolute;
      inset: 0;
      background:
        radial-gradient(ellipse 70% 60% at 20% 30%, rgba(0,137,123,0.25) 0%, transparent 65%),
        radial-gradient(ellipse 50% 50% at 80% 80%, rgba(255,107,53,0.15) 0%, transparent 60%);
      pointer-events: none;
    }

    /* Decorative mandala ring */
    .mandala {
      position: absolute;
      right: -80px;
      bottom: -80px;
      width: 380px;
      height: 380px;
      border-radius: 50%;
      border: 1.5px solid rgba(255,179,0,0.12);
      box-shadow:
        0 0 0 30px rgba(255,179,0,0.06),
        0 0 0 60px rgba(255,179,0,0.04),
        0 0 0 90px rgba(255,179,0,0.025);
      animation: spin 40s linear infinite;
    }
    .mandala::after {
      content: '';
      position: absolute;
      inset: 20px;
      border-radius: 50%;
      border: 1px dashed rgba(0,191,165,0.2);
    }

    @keyframes spin { to { transform: rotate(360deg); } }

    /* Floating particles */
    .particles {
      position: absolute;
      inset: 0;
      pointer-events: none;
    }
    .particle {
      position: absolute;
      border-radius: 50%;
      animation: float 6s ease-in-out infinite;
    }
    .particle:nth-child(1) { width: 6px; height: 6px; background: var(--saffron); opacity: 0.6; top: 15%; left: 20%; animation-delay: 0s; }
    .particle:nth-child(2) { width: 4px; height: 4px; background: var(--gold); opacity: 0.5; top: 55%; left: 10%; animation-delay: 1.5s; }
    .particle:nth-child(3) { width: 5px; height: 5px; background: var(--teal-light); opacity: 0.5; top: 75%; left: 40%; animation-delay: 3s; }
    .particle:nth-child(4) { width: 3px; height: 3px; background: var(--saffron); opacity: 0.4; top: 30%; left: 60%; animation-delay: 4.5s; }
    .particle:nth-child(5) { width: 5px; height: 5px; background: var(--gold); opacity: 0.5; top: 85%; left: 15%; animation-delay: 2s; }

    @keyframes float {
      0%, 100% { transform: translateY(0) scale(1); }
      50% { transform: translateY(-18px) scale(1.15); }
    }

    /* Brand logo area */
    .brand {
      position: relative;
      z-index: 2;
      display: flex;
      align-items: center;
      gap: 14px;
      margin-bottom: 56px;
      animation: slideDown 0.7s ease both;
    }
    .brand-icon {
      width: 48px;
      height: 48px;
      border-radius: 12px;
      background: linear-gradient(135deg, var(--saffron), var(--gold));
      display: flex;
      align-items: center;
      justify-content: center;
      box-shadow: 0 4px 20px rgba(255,107,53,0.4);
    }
    .brand-icon svg { width: 26px; height: 26px; fill: white; }
    .brand-name {
      font-family: 'Playfair Display', serif;
      font-size: 22px;
      color: white;
      letter-spacing: 0.02em;
    }
    .brand-name span { color: var(--gold); }

    .hero-text {
      position: relative;
      z-index: 2;
      animation: slideUp 0.8s 0.1s ease both;
    }
    .hero-text h2 {
      font-family: 'Playfair Display', serif;
      font-size: clamp(28px, 3.5vw, 44px);
      color: white;
      line-height: 1.2;
      margin-bottom: 20px;
    }
    .hero-text h2 span {
      background: linear-gradient(90deg, var(--saffron), var(--gold));
      -webkit-background-clip: text;
      background-clip: text;
      -webkit-text-fill-color: transparent;
      color: transparent;
    }
    .hero-text p {
      font-size: 15px;
      color: rgba(255,255,255,0.6);
      line-height: 1.7;
      max-width: 380px;
    }

    /* Stats strip */
    .stats {
      position: relative;
      z-index: 2;
      display: flex;
      gap: 32px;
      margin-top: 52px;
      animation: slideUp 0.9s 0.2s ease both;
    }
    .stat {
      display: flex;
      flex-direction: column;
      gap: 4px;
    }
    .stat-value {
      font-family: 'Playfair Display', serif;
      font-size: 28px;
      color: var(--gold);
    }
    .stat-label {
      font-size: 12px;
      color: rgba(255,255,255,0.45);
      text-transform: uppercase;
      letter-spacing: 0.08em;
    }

    /* Divider line */
    .stat-divider {
      width: 1px;
      background: rgba(255,255,255,0.1);
      align-self: stretch;
    }

    /* ── RIGHT PANEL ── */
    .right-panel {
      background: var(--cream);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 40px 32px;
      position: relative;
      overflow-y: auto;
    }

    .right-panel::before {
      content: '';
      position: absolute;
      top: -100px;
      right: -100px;
      width: 300px;
      height: 300px;
      border-radius: 50%;
      background: radial-gradient(circle, rgba(255,107,53,0.06), transparent 70%);
      pointer-events: none;
    }

    .login-card {
      width: 100%;
      max-width: 400px;
      animation: fadeIn 0.9s 0.15s ease both;
    }

    @keyframes fadeIn { from { opacity: 0; transform: translateY(24px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes slideDown { from { opacity: 0; transform: translateY(-20px); } to { opacity: 1; transform: translateY(0); } }
    @keyframes slideUp { from { opacity: 0; transform: translateY(20px); } to { opacity: 1; transform: translateY(0); } }

    .card-header {
      margin-bottom: 36px;
    }
    .card-header h1 {
      font-family: 'Playfair Display', serif;
      font-size: 30px;
      color: var(--text-main);
      margin-bottom: 8px;
    }
    .card-header p {
      font-size: 14px;
      color: var(--text-muted);
    }

    /* Accent bar */
    .accent-bar {
      width: 48px;
      height: 4px;
      border-radius: 2px;
      background: linear-gradient(90deg, var(--saffron), var(--gold));
      margin-bottom: 14px;
    }

    .error-msg {
      background: #fff0ee;
      border: 1px solid #ffd0c8;
      color: #c0392b;
      padding: 11px 14px;
      border-radius: 10px;
      margin-bottom: 20px;
      font-size: 13.5px;
      display: flex;
      align-items: center;
      gap: 8px;
    }
    .error-msg::before { content: '⚠'; font-size: 15px; }

    .form-group {
      margin-bottom: 20px;
    }

    label {
      display: block;
      font-size: 12.5px;
      font-weight: 600;
      color: var(--text-main);
      text-transform: uppercase;
      letter-spacing: 0.07em;
      margin-bottom: 8px;
    }

    .input-wrap {
      position: relative;
    }

    .input-wrap svg {
      position: absolute;
      left: 14px;
      top: 50%;
      transform: translateY(-50%);
      width: 17px;
      height: 17px;
      color: var(--text-muted);
      pointer-events: none;
    }

    input[type="email"],
    input[type="password"],
    input[type="text"] {
      width: 100%;
      padding: 13px 14px 13px 42px;
      border: 1.5px solid var(--border);
      border-radius: 10px;
      font-size: 14.5px;
      font-family: 'DM Sans', sans-serif;
      color: var(--text-main);
      background: white;
      outline: none;
      transition: border-color 0.2s, box-shadow 0.2s;
    }

    input:focus {
      border-color: var(--teal);
      box-shadow: 0 0 0 3px rgba(0,137,123,0.1);
    }

    input::placeholder { color: #c4c9d4; }

    .form-footer {
      display: flex;
      align-items: center;
      justify-content: space-between;
      margin-bottom: 22px;
    }

    .remember {
      display: flex;
      align-items: center;
      gap: 7px;
      font-size: 13px;
      color: var(--text-muted);
      cursor: pointer;
    }

    .remember input[type="checkbox"] {
      width: 15px;
      height: 15px;
      accent-color: var(--teal);
      padding: 0;
      border-radius: 4px;
    }

    .forgot {
      font-size: 13px;
      color: var(--teal);
      text-decoration: none;
      font-weight: 500;
    }
    .forgot:hover { text-decoration: underline; }

    .btn {
      width: 100%;
      padding: 14px;
      border: none;
      border-radius: 10px;
      font-size: 15px;
      font-weight: 600;
      font-family: 'DM Sans', sans-serif;
      cursor: pointer;
      transition: transform 0.15s, box-shadow 0.15s, opacity 0.15s;
    }
    .btn:hover { transform: translateY(-2px); }
    .btn:active { transform: translateY(0); }

    .btn-primary {
      background: linear-gradient(135deg, var(--teal) 0%, #004D40 100%);
      color: white;
      box-shadow: 0 6px 24px rgba(0,137,123,0.35);
      position: relative;
      overflow: hidden;
    }
    .btn-primary::after {
      content: '';
      position: absolute;
      inset: 0;
      background: linear-gradient(135deg, rgba(255,255,255,0.1), transparent);
      pointer-events: none;
    }
    .btn-primary:hover { box-shadow: 0 8px 30px rgba(0,137,123,0.45); }

    .divider {
      display: flex;
      align-items: center;
      gap: 12px;
      margin: 22px 0;
      color: var(--text-muted);
      font-size: 12.5px;
    }
    .divider::before, .divider::after {
      content: '';
      flex: 1;
      height: 1px;
      background: var(--border);
    }

    .btn-google {
      background: white;
      border: 1.5px solid var(--border);
      color: var(--text-main);
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 10px;
      box-shadow: 0 2px 8px rgba(0,0,0,0.05);
    }
    .btn-google:hover { box-shadow: 0 4px 16px rgba(0,0,0,0.1); border-color: #c0c7d0; }
    .btn-google svg { width: 19px; height: 19px; flex-shrink: 0; }

    .bottom-link {
      text-align: center;
      margin-top: 24px;
      font-size: 14px;
      color: var(--text-muted);
    }
    .bottom-link a {
      color: var(--saffron);
      text-decoration: none;
      font-weight: 600;
    }
    .bottom-link a:hover { text-decoration: underline; }

    /* Trust badges */
    .trust-row {
      display: flex;
      align-items: center;
      justify-content: center;
      gap: 16px;
      margin-top: 28px;
      padding-top: 20px;
      border-top: 1px solid var(--border);
    }
    .badge {
      display: flex;
      align-items: center;
      gap: 5px;
      font-size: 11.5px;
      color: var(--text-muted);
    }
    .badge svg { width: 14px; height: 14px; color: var(--teal); }

    /* ── RESPONSIVE ── */
    @media (max-width: 768px) {
      body {
        grid-template-columns: 1fr;
        grid-template-rows: auto 1fr;
        overflow: auto;
      }

      .left-panel {
        padding: 36px 28px 40px;
        min-height: unset;
      }

      .mandala { display: none; }

      .hero-text h2 { font-size: 26px; }
      .hero-text p { font-size: 13.5px; }

      .stats { gap: 20px; margin-top: 32px; }
      .stat-value { font-size: 22px; }

      .right-panel { padding: 36px 20px 48px; }

      .login-card { max-width: 100%; }
    }

    @media (max-width: 420px) {
      .stats { flex-wrap: wrap; gap: 16px 28px; }
      .stat-divider { display: none; }
      .form-footer { flex-direction: column; align-items: flex-start; gap: 10px; }
    }
  </style>
</head>
<body>

  <!-- LEFT DECORATIVE PANEL -->
  <div class="left-panel">
    <div class="particles">
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
      <div class="particle"></div>
    </div>
    <div class="mandala"></div>

    <div class="brand">
      <div class="brand-icon">
        <svg viewBox="0 0 24 24" xmlns="http://www.w3.org/2000/svg">
          <path d="M12 2L2 7l10 5 10-5-10-5zM2 17l10 5 10-5M2 12l10 5 10-5"/>
        </svg>
      </div>
      <div class="brand-name">MSE<span>Connect</span></div>
    </div>

    <div class="hero-text">
      <h2>Empowering India's<br><span>Micro & Small</span><br>Enterprises</h2>
      <p>AI-powered aggregation platform bridging MSEs with buyers, logistics, and government schemes — one district at a time.</p>
    </div>

    <div class="stats">
      <div class="stat">
        <div class="stat-value">6.3Cr+</div>
        <div class="stat-label">MSEs in India</div>
      </div>
      <div class="stat-divider"></div>
      <div class="stat">
        <div class="stat-value">ODOP</div>
        <div class="stat-label">Aligned</div>
      </div>
      <div class="stat-divider"></div>
      <div class="stat">
        <div class="stat-value">GeM</div>
        <div class="stat-label">Integrated</div>
      </div>
    </div>
  </div>

  <!-- RIGHT LOGIN PANEL -->
  <div class="right-panel">
    <div class="login-card">

      <div class="card-header">
        <div class="accent-bar"></div>
        <h1>Welcome Back</h1>
        <p>Sign in to your enterprise account</p>
      </div>

      <% if (typeof error !== 'undefined' && error && error.length > 0) { %>
        <div class="error-msg"><%= error %></div>
      <% } %>

      <form action="/login" method="POST">
        <div class="form-group">
          <label>Email Address</label>
          <div class="input-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="2" y="4" width="20" height="16" rx="2"/><path d="m22 7-8.97 5.7a1.94 1.94 0 0 1-2.06 0L2 7"/>
            </svg>
            <input type="email" name="email" placeholder="you@example.com" required autocomplete="email" />
          </div>
        </div>

        <div class="form-group">
          <label>Password</label>
          <div class="input-wrap">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round">
              <rect x="3" y="11" width="18" height="11" rx="2"/><circle cx="12" cy="16" r="1"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/>
            </svg>
            <input type="password" name="password" placeholder="••••••••" required autocomplete="current-password" />
          </div>
        </div>

        <div class="form-footer">
          <label class="remember">
            <input type="checkbox" name="remember"> Keep me signed in
          </label>
          <a href="/forgot-password" class="forgot">Forgot password?</a>
        </div>

        <button class="btn btn-primary" type="submit">Sign In to Dashboard</button>
      </form>

      <div class="divider">or</div>

      <a href="/auth/google" style="text-decoration:none;">
        <button class="btn btn-google" type="button">
          <svg viewBox="0 0 48 48">
            <path fill="#EA4335" d="M24 9.5c3.54 0 6.71 1.22 9.21 3.6l6.85-6.85C35.9 2.38 30.47 0 24 0 14.62 0 6.51 5.38 2.56 13.22l7.98 6.19C12.43 13.72 17.74 9.5 24 9.5z"/>
            <path fill="#4285F4" d="M46.98 24.55c0-1.57-.15-3.09-.38-4.55H24v9.02h12.94c-.58 2.96-2.26 5.48-4.78 7.18l7.73 6c4.51-4.18 7.09-10.36 7.09-17.65z"/>
            <path fill="#FBBC05" d="M10.53 28.59a14.5 14.5 0 0 1 0-9.18l-7.98-6.19a24.06 24.06 0 0 0 0 21.56l7.98-6.19z"/>
            <path fill="#34A853" d="M24 48c6.48 0 11.93-2.13 15.89-5.81l-7.73-6c-2.15 1.45-4.92 2.3-8.16 2.3-6.26 0-11.57-4.22-13.47-9.91l-7.98 6.19C6.51 42.62 14.62 48 24 48z"/>
          </svg>
          Continue with Google
        </button>
      </a>

      <div class="bottom-link">
        New enterprise? <a href="/signup">Create an account</a>
      </div>

      <div class="trust-row">
        <div class="badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><path d="M12 22s8-4 8-10V5l-8-3-8 3v7c0 6 8 10 8 10z"/></svg>
          Govt. Verified
        </div>
        <div class="badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><rect x="3" y="11" width="18" height="11" rx="2"/><path d="M7 11V7a5 5 0 0 1 10 0v4"/></svg>
          SSL Secured
        </div>
        <div class="badge">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2"><polyline points="20 6 9 17 4 12"/></svg>
          GeM Aligned
        </div>
      </div>

    </div>
  </div>

</body>
</html>